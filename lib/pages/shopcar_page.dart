import 'package:flutter/material.dart';

class ShopcarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('ShopcarPage'),
      ),
    );
  }
}
